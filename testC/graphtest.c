#include<stdio.h>
struct BOOTINFO { /* 0x0ff0-0x0fff */
	char cyls; /* ?u?[?g?Z?N?^???????f?B?X?N??????? */
	char leds; /* ?u?[?g????L?[?{?[?h??LED???? */
	char vmode; /* ?r?f?I???[?h  ???r?b?g?J???[?? */
	char reserve;
	short scrnx, scrny; /* ?????x */
	char *vram;
};
int main()
{
	struct BOOTINFO *binfo = (struct BOOTINFO *) 0xff00;
	printf("start");
	int x = 0, y = 0;

	for(;x < 30; x++)
	{
		for(y = 0; y < 30; y++)
		binfo->vram[x * 3 + y] = '4';
	}
	printf("ok");
}
